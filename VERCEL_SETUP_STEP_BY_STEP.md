# 📝 Пошаговая инструкция: Создание проектов в Vercel

## Шаг 1: Откройте Vercel Dashboard

1. Перейдите на https://vercel.com
2. Войдите в свой аккаунт (или зарегистрируйтесь)
3. Откройте Dashboard: https://vercel.com/dashboard

## Шаг 2: Создание Backend проекта (API)

### 2.1. Начало создания проекта

1. Нажмите кнопку **"Add New..."** или **"New Project"** в правом верхнем углу
2. Выберите **"Import Git Repository"**
3. Если репозиторий не виден:
   - Нажмите **"Adjust GitHub App Permissions"**
   - Убедитесь, что репозиторий `ahmed11551/prayer-debt-kaza` доступен
   - Или нажмите **"Import"** рядом с репозиторием

### 2.2. Настройка Backend проекта

После выбора репозитория, настройте проект:

**Configure Project:**
- **Project Name**: `prayer-debt-api` (или `prayer-debt-kaza-backend`)
- **Framework Preset**: Выберите **"Other"** из выпадающего списка
- **Root Directory**: Оставьте **пустым** (точка `.` означает корень)
- **Build Command**: Оставьте **пустым**
- **Output Directory**: Оставьте **пустым**
- **Install Command**: `npm install` (или оставьте по умолчанию)

**Environment Variables** (нажмите "Environment Variables" для добавления):

Добавьте следующие переменные (нажмите "Add" для каждой):

```
Name: SUPABASE_URL
Value: your_supabase_url
Environment: Production, Preview, Development (отметьте все)

Name: SUPABASE_ANON_KEY
Value: your_anon_key
Environment: Production, Preview, Development

Name: SUPABASE_SERVICE_ROLE_KEY
Value: your_service_role_key
Environment: Production, Preview, Development

Name: EREPLIKA_BASE_URL
Value: https://bot.e-replika.ru
Environment: Production, Preview, Development

Name: EREPLIKA_API_KEY
Value: your_api_key
Environment: Production, Preview, Development

Name: API_BASE_URL
Value: https://prayer-debt-api.vercel.app
Environment: Production, Preview, Development
(Примечание: обновите это после деплоя на реальный URL)

Name: PORT
Value: 4000
Environment: Production, Preview, Development

Name: CALC_VERSION
Value: 1.0.0
Environment: Production, Preview, Development

Name: MADHAB
Value: hanafi
Environment: Production, Preview, Development
```

### 2.3. Деплой Backend

1. Нажмите кнопку **"Deploy"** внизу страницы
2. Дождитесь завершения деплоя (обычно 1-2 минуты)
3. После успешного деплоя скопируйте **URL проекта** (например: `https://prayer-debt-api-xyz123.vercel.app`)
4. Сохраните этот URL — он понадобится для frontend

### 2.4. Проверка Backend

1. Откройте URL в браузере (например: `https://prayer-debt-api-xyz123.vercel.app/`)
2. Должен вернуться JSON:
   ```json
   {
     "message": "Prayer Debt Calculator API",
     "version": "1.0.0",
     "endpoints": {
       "prayerDebt": "/api/prayer-debt",
       "webhooks": "/api/webhooks",
       "duas": "/api/duas",
       "ai": "/api/ai",
       "goals": "/api/goals",
       "glossary": "/api/glossary",
       "prayerCalendar": "/api/prayer-calendar",
       "friends": "/api/friends"
     }
   }
   ```

3. Если видите этот JSON — ✅ Backend работает правильно!

## Шаг 3: Обновление API_BASE_URL в Backend

1. Вернитесь в Vercel Dashboard
2. Откройте проект `prayer-debt-api`
3. Перейдите в **Settings** → **Environment Variables**
4. Найдите переменную `API_BASE_URL`
5. Нажмите на неё и обновите значение на реальный URL вашего backend (который вы скопировали)
6. Нажмите **Save**
7. Перейдите в **Deployments** и нажмите **"Redeploy"** на последнем деплое

## Шаг 4: Создание Frontend проекта (Web)

### 4.1. Начало создания проекта

1. В Vercel Dashboard нажмите **"Add New..."** или **"New Project"**
2. Выберите **"Import Git Repository"**
3. Выберите тот же репозиторий: `ahmed11551/prayer-debt-kaza`

### 4.2. Настройка Frontend проекта

**Configure Project:**
- **Project Name**: `prayer-debt-web` (или `prayer-debt-kaza-frontend`)
- **Framework Preset**: Должен автоматически определиться как **"Vite"** (если нет — выберите вручную)
- **Root Directory**: Введите `web` (важно! Это папка с frontend кодом)
- **Build Command**: `npm run build` (должен определиться автоматически)
- **Output Directory**: `dist` (должен определиться автоматически)
- **Install Command**: `npm install` (или оставьте по умолчанию)

**Environment Variables:**

Добавьте одну переменную:

```
Name: VITE_API_URL
Value: https://prayer-debt-api-xyz123.vercel.app/api
(Замените на реальный URL вашего backend + /api)
Environment: Production, Preview, Development (отметьте все)
```

**Важно**: URL должен заканчиваться на `/api`!

### 4.3. Деплой Frontend

1. Нажмите кнопку **"Deploy"** внизу страницы
2. Дождитесь завершения деплоя (обычно 2-3 минуты)
3. После успешного деплоя скопируйте **URL проекта** (например: `https://prayer-debt-web-abc456.vercel.app`)

### 4.4. Проверка Frontend

1. Откройте URL в браузере (например: `https://prayer-debt-web-abc456.vercel.app/`)
2. Должен открыться интерфейс приложения:
   - ✅ Заголовок: "Пропущенные намазы (Каза)"
   - ✅ Подзаголовок: "Расчёт по ханафитскому мазхабу"
   - ✅ Вкладки навигации внизу: Расчёт, Мой долг, Сафар, План, Отчёты и т.д.
   - ✅ Зелёный дизайн в стиле Islamic World

3. Если видите этот интерфейс — ✅ Frontend работает правильно!

## Шаг 5: Финальная проверка

### Проверьте работу API из Frontend:

1. Откройте Frontend в браузере
2. Откройте Developer Tools (F12)
3. Перейдите на вкладку **Network**
4. Попробуйте выполнить расчёт на вкладке "Расчёт"
5. Проверьте, что запросы идут на правильный backend URL

### Проверьте все endpoints:

Откройте в браузере (замените URL на ваш):

- ✅ `https://prayer-debt-api-xyz123.vercel.app/` — должен вернуть JSON с endpoints
- ✅ `https://prayer-debt-api-xyz123.vercel.app/api/prayer-debt/snapshot` — должен вернуть 404 или JSON
- ✅ `https://prayer-debt-api-xyz123.vercel.app/api/duas/categories` — должен вернуть категории ду'а

## Шаг 6: Настройка Telegram Mini App (опционально)

После успешного деплоя frontend:

1. Откройте @BotFather в Telegram
2. Выберите вашего бота
3. Отправьте команду: `/newapp` или `/newappshort`
4. Укажите:
   - **Title**: Пропущенные намазы (Каза)
   - **Description**: Расчёт и отслеживание пропущенных намазов
   - **Photo**: (опционально) загрузите иконку
   - **Web App URL**: `https://prayer-debt-web-abc456.vercel.app`
5. Готово! Приложение доступно через вашего бота

## ❌ Решение проблем

### Проблема: Backend возвращает 404

**Решение:**
1. Проверьте, что файл `api/index.ts` существует
2. Проверьте, что `vercel.json` правильно настроен
3. Проверьте логи деплоя в Vercel Dashboard

### Проблема: Frontend не подключается к Backend

**Решение:**
1. Проверьте переменную `VITE_API_URL` в настройках frontend проекта
2. Убедитесь, что URL заканчивается на `/api`
3. Проверьте CORS настройки в backend (должен быть `origin: "*"`)

### Проблема: Ошибка при сборке

**Решение:**
1. Проверьте логи деплоя в Vercel Dashboard
2. Убедитесь, что все зависимости указаны в `package.json`
3. Проверьте, что Node.js версия >= 18.0.0

### Проблема: Переменные окружения не работают

**Решение:**
1. Убедитесь, что переменные добавлены для всех окружений (Production, Preview, Development)
2. После добавления переменных — передеплойте проект
3. Проверьте, что имена переменных написаны правильно (регистр важен!)

## 📞 Нужна помощь?

Если что-то не работает:
1. Проверьте логи деплоя в Vercel Dashboard
2. Проверьте, что все файлы загружены в GitHub
3. Убедитесь, что последний коммит содержит все изменения

## ✅ Чеклист готовности

- [ ] Backend проект создан и задеплоен
- [ ] Backend URL возвращает правильный JSON
- [ ] `API_BASE_URL` обновлён на реальный URL
- [ ] Frontend проект создан и задеплоен
- [ ] `VITE_API_URL` указывает на backend URL + `/api`
- [ ] Frontend открывается и показывает интерфейс приложения
- [ ] Все endpoints работают
- [ ] Telegram Mini App настроен (опционально)

