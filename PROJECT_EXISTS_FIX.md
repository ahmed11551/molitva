# 🔧 Проект уже существует в Vercel - что делать?

## Проблема

Ошибка: **"Проект «prayer-debt-kaza» уже существует, пожалуйста, используйте новое название."**

Это означает, что проект с таким именем уже создан в вашем аккаунте Vercel.

## ✅ Решение: 3 варианта

### Вариант 1: Использовать другое имя (Рекомендуется)

1. В поле **"Название Проекта"** измените имя на:
   - `prayer-debt-api` (для backend)
   - `prayer-debt-backend` (альтернатива)
   - `prayer-debt-kaza-api` (если хотите сохранить часть оригинального имени)

2. Проверьте настройки:
   - **Framework Preset**: должно быть `Other` (не Express!)
   - **Root Directory**: должно быть `./` (корень) для backend
   - **Build Command**: оставьте пустым
   - **Output Directory**: оставьте пустым

3. Нажмите **"Развертывать" (Deploy)**

### Вариант 2: Обновить существующий проект

1. Закройте это окно
2. Откройте https://vercel.com/dashboard
3. Найдите проект `prayer-debt-kaza`
4. Откройте его
5. Перейдите в **Settings** → **Git**
6. Проверьте:
   - Подключен ли правильный репозиторий
   - Правильный ли Root Directory (должен быть `.` для backend)
7. Перейдите в **Deployments**
8. Нажмите **Redeploy**

### Вариант 3: Удалить старый проект и создать новый

1. Откройте https://vercel.com/dashboard
2. Найдите проект `prayer-debt-kaza`
3. Откройте его
4. Перейдите в **Settings** → **General**
5. Прокрутите вниз до **"Danger Zone"**
6. Нажмите **"Delete Project"**
7. Подтвердите удаление
8. Вернитесь к импорту и создайте проект заново

## ⚠️ Важно: Проверьте настройки Framework

Я вижу, что в настройках выбран **Framework Preset: Express**.

**Для backend это неправильно!** Нужно:

1. Измените **Framework Preset** на **`Other`**
2. **Root Directory**: `./` (корень)
3. **Build Command**: оставьте **пустым**
4. **Output Directory**: оставьте **пустым**

## 📋 Правильные настройки для Backend

- **Project Name**: `prayer-debt-api` (или другое уникальное имя)
- **Framework Preset**: `Other` ⚠️ **ВАЖНО!**
- **Root Directory**: `./` (корень проекта)
- **Build Command**: оставьте пустым
- **Output Directory**: оставьте пустым
- **Install Command**: `npm install` (или оставьте по умолчанию)

## 📋 Правильные настройки для Frontend (отдельный проект)

- **Project Name**: `prayer-debt-web` (или другое имя)
- **Framework Preset**: `Vite` (определится автоматически)
- **Root Directory**: `web` ⚠️ **ВАЖНО!**
- **Build Command**: `npm run build`
- **Output Directory**: `dist`

## ✅ Рекомендуемый план действий

1. **Измените имя проекта** на `prayer-debt-api`
2. **Измените Framework Preset** на `Other`
3. **Проверьте Root Directory** = `./`
4. **Добавьте Environment Variables** (после деплоя или в настройках)
5. Нажмите **Deploy**

## 🔍 После деплоя

Проверьте URL проекта:
- Должен вернуться JSON с информацией об API
- Если всё правильно, вы увидите:
  ```json
  {
    "message": "Prayer Debt Calculator API",
    "version": "1.0.0",
    "endpoints": { ... }
  }
  ```

---

**Самый простой способ: измените имя на `prayer-debt-api` и Framework на `Other`!** 🚀

