# ✅ Чеклист проверки деплоя на Vercel

## 🔍 Проблема
На скриншотах показаны другие проекты (`your-backend.vercel.app` и `your-frontend.vercel.app`), а не наш проект "Prayer Debt Calculator".

## 📋 Что нужно сделать

### 1. Проверьте проекты в Vercel Dashboard

1. Откройте https://vercel.com/dashboard
2. Найдите проекты с названиями:
   - `prayer-debt-kaza` (или похожее для backend)
   - `prayer-debt-web` (или похожее для frontend)
3. Если проектов нет — создайте их (см. ниже)

### 2. Создание проектов (если их нет)

#### Backend проект:
1. Нажмите **"Add New Project"** в Vercel
2. Импортируйте репозиторий: `ahmed11551/prayer-debt-kaza`
3. Настройки:
   - **Project Name**: `prayer-debt-api` (или `prayer-debt-kaza-backend`)
   - **Root Directory**: `.` (корень, оставьте пустым)
   - **Framework Preset**: `Other`
   - **Build Command**: оставьте пустым
   - **Output Directory**: оставьте пустым
4. **Environment Variables**:
   ```
   SUPABASE_URL=your_supabase_url
   SUPABASE_ANON_KEY=your_anon_key
   SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
   EREPLIKA_BASE_URL=https://bot.e-replika.ru
   EREPLIKA_API_KEY=your_api_key
   API_BASE_URL=https://prayer-debt-api.vercel.app
   ```
5. Нажмите **Deploy**
6. Скопируйте URL (например: `https://prayer-debt-api.vercel.app`)

#### Frontend проект:
1. Нажмите **"Add New Project"** в Vercel
2. Импортируйте тот же репозиторий: `ahmed11551/prayer-debt-kaza`
3. Настройки:
   - **Project Name**: `prayer-debt-web` (или `prayer-debt-kaza-frontend`)
   - **Root Directory**: `web`
   - **Framework Preset**: `Vite` (автоматически определится)
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
4. **Environment Variables**:
   ```
   VITE_API_URL=https://prayer-debt-api.vercel.app/api
   ```
   (замените на реальный URL вашего backend)
5. Нажмите **Deploy**
6. Скопируйте URL (например: `https://prayer-debt-web.vercel.app`)

### 3. Проверка правильных URL

После деплоя проверьте:

#### Backend:
- Откройте: `https://prayer-debt-api.vercel.app/`
  - Должен вернуть JSON:
    ```json
    {
      "message": "Prayer Debt Calculator API",
      "version": "1.0.0",
      "endpoints": { ... }
    }
    ```

- Откройте: `https://prayer-debt-api.vercel.app/api/prayer-debt/snapshot`
  - Должен вернуть 404 или JSON (это нормально, если нет данных)

#### Frontend:
- Откройте: `https://prayer-debt-web.vercel.app/`
  - Должен открыться интерфейс приложения "Пропущенные намазы (Каза)"
  - Должны быть видны вкладки: Расчёт, Мой долг, Сафар, План, Отчёты и т.д.

### 4. Если видите другие проекты

Если в Vercel Dashboard вы видите проекты с другими названиями:
- `your-backend` — это не наш проект
- `your-frontend` — это не наш проект
- `myfurniture` — это не наш проект

**Решение**: Создайте новые проекты с правильными названиями (см. выше) или переименуйте существующие.

### 5. Обновление переменных окружения

После деплоя backend:
1. Скопируйте реальный URL backend (например: `https://prayer-debt-api-xyz.vercel.app`)
2. Обновите `API_BASE_URL` в настройках backend проекта
3. Обновите `VITE_API_URL` в настройках frontend проекта на `https://prayer-debt-api-xyz.vercel.app/api`
4. Передеплойте оба проекта

## 🎯 Ожидаемый результат

**Backend URL** должен показывать:
```json
{
  "message": "Prayer Debt Calculator API",
  "version": "1.0.0",
  "endpoints": {
    "prayerDebt": "/api/prayer-debt",
    "webhooks": "/api/webhooks",
    "duas": "/api/duas",
    "ai": "/api/ai",
    "goals": "/api/goals",
    "glossary": "/api/glossary",
    "prayerCalendar": "/api/prayer-calendar",
    "friends": "/api/friends"
  }
}
```

**Frontend URL** должен показывать:
- Интерфейс приложения "Пропущенные намазы (Каза)"
- Зелёный дизайн в стиле Islamic World
- Вкладки навигации внизу
- Заголовок "Пропущенные намазы (Каза)"

## ❌ Что НЕ должно быть

- ❌ Код с MongoDB и `cartRoutes`, `userRoutes` (это другой проект)
- ❌ Страница с роботом и "Welcome to our app" (это другой проект)
- ❌ Страница с логотипом "auth" (это другой проект)

## 📞 Если что-то не работает

1. Проверьте логи деплоя в Vercel Dashboard
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что репозиторий `ahmed11551/prayer-debt-kaza` подключен
4. Убедитесь, что последний коммит в GitHub содержит все файлы

