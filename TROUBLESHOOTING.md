# 🔧 Решение проблем с деплоем

## Проблема: Проект не развернулся

### Шаг 1: Проверьте логи деплоя в Vercel

1. Откройте Vercel Dashboard
2. Выберите ваш проект
3. Перейдите в **Deployments**
4. Откройте последний деплой
5. Просмотрите **Logs** - найдите ошибки

### Шаг 2: Частые ошибки и решения

#### Ошибка: "Module not found" или "Cannot find module"

**Решение:**
- Убедитесь, что все зависимости указаны в `package.json`
- Проверьте, что нет импортов несуществующих пакетов

#### Ошибка: "TypeScript compilation error"

**Решение:**
- Проверьте `tsconfig.json`
- Убедитесь, что все типы правильно определены
- Проверьте импорты

#### Ошибка: "Build command failed"

**Решение:**
- Для backend: Build Command должно быть **пустым** (Vercel использует TypeScript напрямую)
- Для frontend: Build Command = `npm run build`

#### Ошибка: "Cannot find api/index.ts"

**Решение:**
- Убедитесь, что файл `api/index.ts` существует в корне проекта
- Проверьте `vercel.json` - путь должен быть `api/index.ts`

### Шаг 3: Проверка конфигурации

#### Backend проект:

- ✅ Root Directory: `.` (корень)
- ✅ Framework Preset: `Other`
- ✅ Build Command: **пусто**
- ✅ Output Directory: **пусто**
- ✅ Файл `api/index.ts` существует
- ✅ Файл `vercel.json` существует

#### Frontend проект:

- ✅ Root Directory: `web`
- ✅ Framework Preset: `Vite`
- ✅ Build Command: `npm run build`
- ✅ Output Directory: `dist`
- ✅ Файл `web/package.json` существует
- ✅ Файл `web/vercel.json` существует

### Шаг 4: Локальная проверка

Проверьте, что проект собирается локально:

```bash
# Backend
npm install
npm run build

# Frontend
cd web
npm install
npm run build
```

Если локально не работает - исправьте ошибки сначала.

### Шаг 5: Проверка файлов в репозитории

Убедитесь, что все файлы загружены в GitHub:

1. Откройте https://github.com/ahmed11551/molitva
2. Проверьте наличие:
   - `api/index.ts`
   - `vercel.json`
   - `package.json`
   - `src/` папка со всеми файлами
   - `web/` папка со всеми файлами

### Шаг 6: Пересоздание проекта

Если ничего не помогает:

1. Удалите проект в Vercel
2. Создайте новый проект
3. Импортируйте репозиторий заново
4. Проверьте все настройки
5. Деплойте

## 📝 Чеклист проверки

- [ ] Все файлы в GitHub
- [ ] `package.json` не содержит несуществующих пакетов
- [ ] `api/index.ts` существует
- [ ] `vercel.json` правильно настроен
- [ ] TypeScript компилируется без ошибок
- [ ] Environment Variables добавлены
- [ ] Root Directory правильный (`.` для backend, `web` для frontend)
- [ ] Framework Preset правильный (`Other` для backend, `Vite` для frontend)

## 🔍 Детальная диагностика

Если проблема не решена, пришлите:
1. Скриншот логов деплоя из Vercel
2. Текст ошибки
3. Настройки проекта (Root Directory, Framework, Build Command)

---

**После исправления - передеплойте проект!**

