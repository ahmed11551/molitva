# 🚀 Деплой на Vercel с нового репозитория

## Шаг 1: Создание нового репозитория на GitHub

1. Откройте https://github.com/new
2. Заполните данные:
   - **Repository name**: `prayer-debt-kaza` (или любое другое имя)
   - **Description**: `Telegram Mini App for calculating missed prayers (Kaza)`
   - **Visibility**: Выберите **Private** или **Public**
   - **НЕ** добавляйте README, .gitignore или лицензию (всё уже есть)
3. Нажмите **"Create repository"**

## Шаг 2: Загрузка кода в новый репозиторий

### Вариант A: Через командную строку

```bash
# В папке проекта
git remote remove origin  # Если был старый remote
git remote add origin https://github.com/YOUR_USERNAME/prayer-debt-kaza.git
git branch -M main
git push -u origin main
```

### Вариант B: Через GitHub Desktop

1. Откройте GitHub Desktop
2. File → Add Local Repository
3. Выберите папку проекта
4. В настройках репозитория укажите новый remote
5. Commit & Push

## Шаг 3: Настройка доступа Vercel к GitHub

1. Откройте https://github.com/settings/installations
2. Найдите **Vercel** в списке
3. Нажмите **Configure**
4. В разделе **"Repository access"**:
   - Выберите **"Only select repositories"**
   - Добавьте ваш новый репозиторий `prayer-debt-kaza`
5. Нажмите **Save**

## Шаг 4: Деплой Backend на Vercel

1. Откройте https://vercel.com/new
2. Нажмите **"Import Git Repository"**
3. Выберите ваш новый репозиторий
4. Настройки проекта:
   - **Project Name**: `prayer-debt-api`
   - **Root Directory**: `.` (оставьте пустым - это корень)
   - **Framework Preset**: `Other` ⚠️ **ВАЖНО!**
   - **Build Command**: оставьте **пустым**
   - **Output Directory**: оставьте **пустым**
   - **Install Command**: `npm install` (или по умолчанию)
5. Нажмите **"Environment Variables"** и добавьте:
   ```
   SUPABASE_URL=your_supabase_url
   SUPABASE_ANON_KEY=your_anon_key
   SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
   EREPLIKA_BASE_URL=https://bot.e-replika.ru
   EREPLIKA_API_KEY=your_api_key
   API_BASE_URL=https://prayer-debt-api.vercel.app
   PORT=4000
   CALC_VERSION=1.0.0
   MADHAB=hanafi
   NODE_ENV=production
   ```
   **Примечание**: `API_BASE_URL` обновите после деплоя на реальный URL
6. Нажмите **"Deploy"**
7. Дождитесь завершения (1-2 минуты)
8. Скопируйте URL (например: `https://prayer-debt-api-xyz.vercel.app`)

## Шаг 5: Обновление API_BASE_URL

1. После деплоя backend скопируйте реальный URL
2. В Vercel Dashboard откройте проект `prayer-debt-api`
3. Перейдите в **Settings** → **Environment Variables**
4. Найдите `API_BASE_URL` и обновите на реальный URL
5. Сохраните
6. Перейдите в **Deployments** → **Redeploy** последнего деплоя

## Шаг 6: Деплой Frontend на Vercel

1. В Vercel Dashboard нажмите **"Add New Project"**
2. Выберите тот же репозиторий
3. Настройки проекта:
   - **Project Name**: `prayer-debt-web`
   - **Root Directory**: `web` ⚠️ **ВАЖНО!**
   - **Framework Preset**: `Vite` (определится автоматически)
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
   - **Install Command**: `npm install` (или по умолчанию)
4. Нажмите **"Environment Variables"** и добавьте:
   ```
   VITE_API_URL=https://prayer-debt-api-xyz.vercel.app/api
   ```
   (замените на реальный URL вашего backend + `/api`)
5. Нажмите **"Deploy"**
6. Дождитесь завершения (2-3 минуты)
7. Скопируйте URL (например: `https://prayer-debt-web-abc.vercel.app`)

## Шаг 7: Проверка деплоя

### Backend проверка

Откройте: `https://your-backend.vercel.app/`

Должен вернуться JSON:
```json
{
  "message": "Prayer Debt Calculator API",
  "version": "1.0.0",
  "endpoints": {
    "prayerDebt": "/api/prayer-debt",
    "webhooks": "/api/webhooks",
    "duas": "/api/duas",
    "ai": "/api/ai",
    "goals": "/api/goals",
    "glossary": "/api/glossary",
    "prayerCalendar": "/api/prayer-calendar",
    "friends": "/api/friends"
  }
}
```

### Frontend проверка

Откройте: `https://your-frontend.vercel.app/`

Должно открыться:
- ✅ Интерфейс "Пропущенные намазы (Каза)"
- ✅ Зелёный дизайн
- ✅ Вкладки навигации работают

## Шаг 8: Создание Telegram бота

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду: `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: `Prayer Debt Calculator`)
   - Введите username бота (например: `prayer_debt_bot`)
4. Сохраните **Bot Token** (понадобится для будущих интеграций)

## Шаг 9: Создание Mini App

1. В @BotFather отправьте команду: `/newapp` или `/newappshort`
2. Выберите вашего бота из списка
3. Заполните данные:
   - **Title**: `Пропущенные намазы (Каза)`
   - **Description**: `Расчёт и отслеживание пропущенных намазов`
   - **Photo**: (опционально) загрузите иконку
   - **Web App URL**: `https://prayer-debt-web-abc.vercel.app` (ваш frontend URL)
   - **Short name**: (опционально) короткое имя
4. Нажмите **Done**

## Шаг 10: Проверка Mini App

1. Откройте вашего бота в Telegram
2. Нажмите на кнопку **"Open App"** или отправьте `/start`
3. Должно открыться ваше приложение
4. Проверьте работу всех функций

## ✅ Чеклист готовности

- [ ] Новый репозиторий создан на GitHub
- [ ] Код загружен в репозиторий
- [ ] Vercel имеет доступ к репозиторию
- [ ] Backend проект создан и задеплоен
- [ ] Backend URL работает (возвращает JSON)
- [ ] API_BASE_URL обновлён на реальный URL
- [ ] Frontend проект создан и задеплоен
- [ ] VITE_API_URL указывает на backend URL + `/api`
- [ ] Frontend URL работает (открывается интерфейс)
- [ ] Telegram бот создан через @BotFather
- [ ] Mini App подключен к frontend URL
- [ ] Mini App работает в Telegram

## 🐛 Решение проблем

### Ошибка: "Project already exists"
- Используйте другое имя проекта (например: `prayer-debt-api-v2`)

### Ошибка: "Repository not found"
- Проверьте доступ Vercel к репозиторию (Шаг 3)
- Убедитесь, что репозиторий добавлен в настройках Vercel GitHub App

### Ошибка: 404 на backend
- Проверьте Root Directory (должен быть `.` для backend)
- Проверьте Framework Preset (должен быть `Other`)

### Ошибка: 404 на frontend
- Проверьте Root Directory (должен быть `web` для frontend)
- Проверьте, что `VITE_API_URL` указан правильно

### Mini App не открывается
- Убедитесь, что frontend URL работает в браузере
- Проверьте, что URL начинается с `https://`
- Убедитесь, что CORS настроен правильно (в коде уже есть `origin: "*"`)

## 📝 Важные замечания

1. **Backend и Frontend - это 2 разных проекта** в Vercel
2. **Root Directory критичен**: `.` для backend, `web` для frontend
3. **Framework Preset**: `Other` для backend, `Vite` для frontend
4. **Environment Variables**: добавьте для всех окружений (Production, Preview, Development)
5. **API_BASE_URL**: обновите после деплоя backend на реальный URL

## 🎉 Готово!

После выполнения всех шагов:
- ✅ Backend работает на Vercel
- ✅ Frontend работает на Vercel
- ✅ Telegram бот создан
- ✅ Mini App подключен и работает

---

**Всё готово к деплою! Следуйте инструкциям выше.** 🚀

