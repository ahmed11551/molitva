# 🔧 Исправление ошибки 404 на Vercel

## Проблема

При открытии `prayer-debt-kaza.vercel.app` возвращается ошибка 404.

## Причины

1. **Проект задеплоен как frontend вместо backend**
2. **Неправильный Root Directory в настройках Vercel**
3. **Отсутствует файл `api/index.ts` в деплое**

## ✅ Решение

### Вариант 1: Проверьте настройки проекта в Vercel

1. Откройте https://vercel.com/dashboard
2. Найдите проект `prayer-debt-kaza`
3. Откройте **Settings** → **General**
4. Проверьте:
   - **Root Directory**: должно быть **пустым** (`.`) или **НЕ** `web`
   - **Framework Preset**: должно быть `Other` (не Vite!)
   - **Build Command**: должно быть **пустым**
   - **Output Directory**: должно быть **пустым**

### Вариант 2: Пересоздайте проект правильно

1. **Удалите текущий проект** (или создайте новый с другим именем)
2. Создайте новый проект:
   - Импортируйте репозиторий: `ahmed11551/prayer-debt-kaza`
   - **Project Name**: `prayer-debt-api` (не `prayer-debt-kaza`)
   - **Root Directory**: `.` (оставьте **пустым**)
   - **Framework Preset**: `Other`
   - **Build Command**: оставьте **пустым**
   - **Output Directory**: оставьте **пустым**
3. Добавьте Environment Variables
4. Нажмите **Deploy**

### Вариант 3: Используйте правильную структуру папок

Если Vercel не видит `api/index.ts`, убедитесь, что:

1. Файл `api/index.ts` существует в репозитории
2. Файл `vercel.json` находится в корне проекта
3. В `vercel.json` указан правильный путь: `"dest": "/api/index.ts"`

## 🔍 Проверка

После исправления:

1. Откройте URL проекта (например: `https://prayer-debt-api.vercel.app/`)
2. Должен вернуться JSON:
   ```json
   {
     "message": "Prayer Debt Calculator API",
     "version": "1.0.0",
     "endpoints": { ... }
   }
   ```

## ⚠️ Важно

**Backend и Frontend - это ДВА РАЗНЫХ проекта в Vercel:**

- **Backend**: Root = `.`, Framework = `Other`
- **Frontend**: Root = `web`, Framework = `Vite`

Если вы задеплоили один проект с Root = `web`, это frontend, и он не будет работать как API!

## 📝 Быстрое исправление

1. Создайте **новый проект** в Vercel с именем `prayer-debt-api`
2. Root Directory: `.` (корень)
3. Framework: `Other`
4. Деплойте

Или переименуйте текущий проект и создайте отдельный для frontend.

